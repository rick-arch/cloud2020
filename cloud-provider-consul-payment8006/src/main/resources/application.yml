server:
  port: 8006

spring:
  application:
    name: cloud-provider-consul-payment
  cloud:
    consul:
      #consul注册中心地址
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}